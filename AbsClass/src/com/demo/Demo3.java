package com.demo;

public interface Demo3 { //it is interface
	public void add();	 //in interface there can be only abstract method but not method body
						 //interface internally adds abstract keyword to method 
}
